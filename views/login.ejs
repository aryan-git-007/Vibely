<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Login Page</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-r from-zinc-900 to-zinc-800 min-h-screen flex items-center justify-center">
  <div class="bg-zinc-900 p-8 rounded-2xl shadow-2xl w-full max-w-md border border-zinc-700">
    <h2 class="text-4xl font-bold text-white text-center mb-6">Welcome Back</h2>
    <p class="text-zinc-400 text-center mb-8 text-sm">Please login to your account</p>

    <form action="/login" method="POST" class="space-y-5">
      <div>
        <label class="block mb-1 text-sm text-zinc-400">Email</label>
        <input 
          class="w-full px-4 py-2 rounded-lg bg-zinc-800 border border-zinc-700 text-white focus:outline-none focus:ring-2 focus:ring-blue-500" 
          type="email" 
          name="email" 
          placeholder="Enter your email" 
          required
        />
      </div>

      <div>
        <label class="block mb-1 text-sm text-zinc-400">Password</label>
        <input 
          class="w-full px-4 py-2 rounded-lg bg-zinc-800 border border-zinc-700 text-white focus:outline-none focus:ring-2 focus:ring-blue-500" 
          type="password" 
          name="password" 
          placeholder="Enter your password" 
          required
        />
      </div>

      <button 
        type="submit" 
        class="w-full bg-blue-600 hover:bg-blue-700 transition-all duration-200 py-2 rounded-lg font-semibold text-white"
      >
        Login
      </button>
    </form>

    <p class="text-center text-sm text-zinc-400 mt-6">
      Don’t have an account? 
      <a href="/create" class="text-blue-400 hover:underline">Create one</a>
    </p>
  </div>
</body>
</html>
